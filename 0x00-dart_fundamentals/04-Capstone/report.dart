import 'package:intl/intl.dart';
import 'package:characters/characters.dart';

/*
void main(List<String> arguments) {
  if(arguments.length < 3) {
    print('Not enough arguments. Try again.');
  } else {
    print(helloUser(arguments[0]));
  }
  print(DateFormat.EEEEMMMMdy().format(DateTime.now()));
  print('${arguments[1]}\nTitle has ${arguments[1].length} characters.\n');
}

helloUser(String name) {
  String hello;
  int currentHour = DateTime.now().hour;
  switch (currentHour){
    case > 11 && < 19:
      hello = 'Good afternoon, $name!';
      break;
    case > 4 && <= 11:
      hello = 'Good morning, $name!';
      break;
    default:
      hello = 'Good night, $name!';
      break;
    }
    return hello;
  }

tasks(List<String> args) {
  List<String> taskList = [];
  int completedTasks = 0;
  taskList.forEach() { {
    if(task.value == "true") {
      completedTasks++;
      print("✅ ${task.key}");
    } else {
      print("❌ ${task.key}");
    }
  }
  print("\nYou completed $completedTasks out of 3 tasks.");
  }
}
*/